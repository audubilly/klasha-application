
#Endpoints
dev.url=https://countriesnow.space
base.url=/api/v0.1/countries
population.cities.url=${dev.url}${base.url}/population/cities
population.url=${dev.url}${base.url}/population
capital.url=${dev.url}${base.url}/capital
state.city.url=${dev.url}${base.url}/state/cities
state.url=${dev.url}${base.url}/states
currency.url=${dev.url}${base.url}/currency


spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.datasource.driver-class-name=org.h2.Driver
